# Solana RPC endpoint. Defaults to the public mainnet-beta endpoint if not set.
# Use a private RPC (e.g. Helius, QuickNode) is required for domain resolution.
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com
